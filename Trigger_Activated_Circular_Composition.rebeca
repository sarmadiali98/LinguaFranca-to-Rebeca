reactiveclass Node(100) {
    knownrebecs {
        Node next;
    }

    statevars {
        int recievedMsg;
    }
    	
    Node() {
        self.sendMsg();
    }
    
    msgsrv sendMsg() {
        int msg;
        msg = 1;
        next.rcvMsg(msg);
    }

    msgsrv rcvMsg(int msg) {
        recievedMsg = msg;
        self.sendMsg();
    }
}

main {
    Node node0(node1):();
    Node node1(node2):();
    Node node2(node3):();
    Node node3(node0):();
}